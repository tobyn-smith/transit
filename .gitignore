# raw downloads, fetched by scripts/00_download.R, not committed
data/raw/

# R
.Rproj.user/
.Rhistory
.RData
.Ruserdata

# renv library (lockfile is committed, library is not)
renv/library/
renv/staging/

# quarto build output (the site is deployed from CI, not committed)
/.quarto/
/docs/

# layers regenerated by scripts/ (the committed source CSVs stay tracked)
data/processed/*.gpkg
data/processed/exposure_ranking.csv

# OS
.DS_Store
Thumbs.db
